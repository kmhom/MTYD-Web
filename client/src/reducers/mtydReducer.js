//Rename this reducer if you want, can also make new reducers by adding to the /reducers/index.js list

// import actions

const initialState = {

};

export default function(state = initialState, action) {
    switch(action.type) {
        //Cases for different actions
        default:
            return state;
    }
}
